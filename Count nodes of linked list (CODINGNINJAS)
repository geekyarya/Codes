public class Solution {
    public static int length(Node head){
        if(head==null)
        return 0;
        int c=1;
        while(head.next!=null && head.data!=-1)
        {
            c++;
            head=head.next;
        }
        return c;
    }
}
